attack:
  name: ring_a_bell
  # InversePrompt.ipynb defaults (not a concept-specific paper sweep).
  population_size: 200
  generations: 3000
  mutation_rate: 0.25
  crossover_rate: 0.5
  prompt_length: 16
  coefficient: 3.0
  concept_pairs_path: data/ring_a_bell/concept_pairs.json
  # Execution settings; microbatching preserves per-candidate fitness.
  device: null
  encoder_revision: null
  batch_size: 32
  log_interval: 50
