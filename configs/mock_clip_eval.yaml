model:
  name: mock

attack:
  name: groot_lite
  decompositions_path: data/groot_decompositions.yaml

evaluation:
  image_clip:
    enabled: true
    model_id: openai/clip-vit-base-patch32
    threshold: 0.25
